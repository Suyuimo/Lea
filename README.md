# Lea
Modular Signal Bot

## Configuration

Copy the example config and adjust it:
```bash
cp config/lea.example.yml config/lea.yml
```

Start lea with following command:
```bash
./gradlew :lea-core:run
```
